generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model User {
  id         String @id @default(uuid())
  github_id  Int
  name       String
  login      String
  avatar_url String

  @@map("users")
}
